# Contributing to OIDC Redirector

Спасибо за интерес к проекту! 🎉

## Как внести вклад

### 🐛 Сообщить о баге

1. Проверьте, что баг еще не был [зарепорчен](https://github.com/YOUR_USERNAME/oidc-redirector/issues)
2. Создайте новый issue с:
   - Четким описанием проблемы
   - Шагами для воспроизведения
   - Ожидаемым и фактическим поведением
   - Версией Chrome и расширения
   - Скриншотами (если применимо)

### ✨ Предложить новую функцию

1. Создайте issue с тегом `enhancement`
2. Опишите:
   - Зачем нужна функция
   - Как она должна работать
   - Примеры использования

### 🔧 Отправить Pull Request

1. **Fork репозитория**
```bash
git clone https://github.com/YOUR_USERNAME/oidc-redirector.git
cd oidc-redirector
```

2. **Создайте feature branch**
```bash
git checkout -b feature/your-feature-name
```

3. **Сделайте изменения**
- Пишите понятный код
- Следуйте существующему стилю
- Добавьте комментарии где нужно

4. **Протестируйте**
- Загрузите расширение в Chrome
- Проверьте все функции
- Убедитесь, что ничего не сломалось

5. **Commit изменения**
```bash
git add .
git commit -m "feat: add awesome feature"
```

Используйте [Conventional Commits](https://www.conventionalcommits.org/):
- `feat:` — новая функция
- `fix:` — исправление бага
- `docs:` — изменения в документации
- `style:` — форматирование кода
- `refactor:` — рефакторинг
- `test:` — тесты
- `chore:` — рутинные задачи

6. **Push в ваш fork**
```bash
git push origin feature/your-feature-name
```

7. **Создайте Pull Request**
- Перейдите на GitHub
- Нажмите "New Pull Request"
- Выберите ваш branch
- Опишите изменения

### 📋 Checklist для PR

- [ ] Код работает и протестирован
- [ ] Следует стилю проекта
- [ ] Обновлена документация (если нужно)
- [ ] Обновлена версия в `manifest.json` (если нужно)
- [ ] Добавлены комментарии к сложному коду
- [ ] Нет console.log в продакшен коде

## 🎨 Стиль кода

### JavaScript
- Используйте ES6+ синтаксис
- Предпочитайте `const` и `let` вместо `var`
- Используйте async/await вместо callbacks
- Отступы: 2 пробела

### Комментарии
```javascript
// Хорошо: краткие и понятные
// Применяем правила редиректа
await applyRedirectRules(enable);

// Плохо: избыточные
// Эта функция применяет правила редиректа используя параметр enable
await applyRedirectRules(enable);
```

### Именование
- Функции: `camelCase`
- Константы: `UPPER_CASE`
- Файлы: `lowercase.js` или `kebab-case.js`

## 🧪 Тестирование

Перед отправкой PR:

1. Загрузите расширение в Chrome
2. Проверьте:
   - [ ] Сохранение настроек
   - [ ] Включение/выключение редиректа
   - [ ] Работу горячей клавиши
   - [ ] Редирект с разными URL
   - [ ] Перезагрузку браузера

## 📝 Документация

При добавлении функций обновите:
- README.md — описание функции
- Комментарии в коде
- Примеры использования

## 🤔 Вопросы?

- Создайте [Discussion](https://github.com/YOUR_USERNAME/oidc-redirector/discussions)
- Спросите в issue
- Посмотрите существующие PR для примера

## 📜 Лицензия

Отправляя PR, вы соглашаетесь с лицензией MIT.

---

Спасибо за помощь в улучшении проекта! 🙏
